# GUI для статистического анализа

Визуальная оболочка на Qt для системы статистического анализа с методами MLS метод.

## Возможности

### Основной функционал:
- **MLE оценка параметров** - нормальное и Вейбулла распределения
- **MLS метод MLS метод** - оценка через порядковые статистики
- **Критерий Граббса** - выявление выбросов
- **F-критерий Фишера** - сравнение дисперсий
- **t-критерий Стьюдента** - сравнение средних (равные/неравные дисперсии)
- **Доверительные интервалы** - для μ, σ², σ
- **Персентили** - с доверительными интервалами

### Интерфейс:

#### Левая панель (параметры):
- Выбор типа анализа
- Настройка уровня значимости α
- Список входных файлов
- Загрузка данных
- Статистика данных

#### Правая панель (результаты):
- **Вкладка "Результаты"** - текстовый вывод
- **Вкладка "Графики"** - визуализация (гистограммы, Q-Q plots, подгонка распределений)
- **Вкладка "Редактор данных"** - просмотр и редактирование данных
- **Вкладка "Выходные файлы"** - просмотр сохраненных результатов

#### Панель управления (внизу):
- **Выполнить** - запуск анализа
- **Очистить** - очистка результатов
- **Сохранить** - сохранение в файл
- **Прогресс-бар** - индикация выполнения

## Сборка и запуск

### Требования:
- Qt 5.15+ или Qt 6.x
- CMake 3.16+
- Boost (для статистических функций)
- C++17 компилятор

### Установка Qt на macOS:
```bash
brew install qt@6
brew install cmake
```

### Сборка:
```bash
cd gui
mkdir build
cd build

# Настроить CMake
cmake ..

# Скомпилировать
make

# Запустить
./StatisticalAnalysisGUI
```

### Альтернативная сборка с qmake:
```bash
cd gui
qmake
make
./StatisticalAnalysisGUI
```

## Структура проекта

```
gui/
├── CMakeLists.txt          # Конфигурация сборки
├── README.md               # Этот файл
│
├── include/                # Заголовочные файлы
│   ├── mainwindow.h        # Главное окно
│   ├── statisticsworker.h  # Рабочий поток для вычислений
│   ├── chartviewer.h       # Виджет для графиков
│   └── dataeditor.h        # Редактор данных
│
├── src/                    # Исходные файлы
│   ├── main.cpp           # Точка входа
│   ├── mainwindow.cpp     # Реализация главного окна
│   ├── statisticsworker.cpp
│   ├── chartviewer.cpp
│   └── dataeditor.cpp
│
├── ui/                     # UI файлы Qt Designer
│   └── mainwindow.ui       # Дизайн главного окна
│
└── resources/              # Ресурсы (иконки, изображения)
    └── resources.qrc
```

## Использование

### 1. Загрузка данных
- Нажмите "Загрузить данные..." или выберите файл из списка
- Данные отобразятся в статистике и редакторе

### 2. Выбор анализа
- Выберите тип анализа из выпадающего списка
- Установите уровень значимости α (по умолчанию 0.05)

### 3. Запуск анализа
- Нажмите "Выполнить"
- Прогресс отобразится на панели внизу
- Результаты появятся на вкладке "Результаты"

### 4. Просмотр графиков
- Переключитесь на вкладку "Графики"
- Графики генерируются автоматически

### 5. Сохранение результатов
- Нажмите "Сохранить результаты"
- Выберите имя и расположение файла

## Примеры использования

### Пример 1: MLE для нормального распределения
1. Загрузите `data_normal.txt`
2. Выберите "MLE - Нормальное распределение"
3. Нажмите "Выполнить"
4. Просмотрите оценки параметров μ и σ

### Пример 2: Критерий Граббса
1. Загрузите данные
2. Выберите "Критерий Граббса (выбросы)"
3. Установите α = 0.05
4. Нажмите "Выполнить"
5. Проверьте, обнаружены ли выбросы

### Пример 3: Сравнение двух выборок
1. Загрузите данные (будут разделены на 2 части автоматически)
2. Выберите "t-критерий Стьюдента"
3. Нажмите "Выполнить"
4. Программа автоматически выберет подходящий метод

## Интеграция с существующим кодом

GUI использует существующую библиотеку статистических функций:
- `mle_methods.h/cpp` - MLE/MLS методы
- `statistical_tests.h/cpp` - Критерии
- `confidence_intervals.h/cpp` - Доверительные интервалы
- `boost_distributions.h/cpp` - Обертки Boost.Math

Все вычисления выполняются в отдельном потоке (`StatisticsWorker`), чтобы не блокировать интерфейс.

## Горячие клавиши

- **Ctrl+O** - Открыть данные
- **Ctrl+S** - Сохранить результаты
- **Ctrl+Q** - Выход
- **F1** - Справка

## Известные ограничения

1. Для тестов с двумя выборками (Fisher, Student) данные автоматически делятся пополам
2. Графики генерируются автоматически и не настраиваются интерактивно
3. Редактор данных поддерживает только простые числовые значения

## Будущие улучшения

- [ ] Поддержка цензурированных данных через GUI
- [ ] Интерактивная настройка графиков
- [ ] Экспорт графиков в различные форматы
- [ ] Сравнение нескольких выборок
- [ ] История анализов
- [ ] Автосохранение

## Поддержка

При возникновении проблем:
1. Проверьте, что установлены все зависимости
2. Убедитесь, что директории `input/` и `output/` существуют
3. Проверьте формат входных данных (числа, по одному на строку)

## Лицензия

🤖 Generated with Claude Code
